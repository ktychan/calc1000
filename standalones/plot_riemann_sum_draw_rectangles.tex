\documentclass[tikz]{standalone}
\input{../tikz.tex.preamble}
\input{../typography.tex.preamble}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    axis lines = middle, % boxed, middle
    % axis on top,
    axis equal image,
    %
    % domain and range
    %
    % xmin={}, xmax={},
    ymin={0}, % ymax={},
    enlargelimits=true,
    %
    % axis labels
    %
    xlabel={\(x\)}, xlabel style={anchor=west},
    ylabel={\(y\)}, ylabel style={anchor=south},
    label style={at={(ticklabel* cs:1), font=\footnotesize}},
    %
    % ticks
    %
    xtick={-1, 0, 1, 2}, 
    ytick={0, 1, ..., 4}, yticklabels={0,1,,3,},
    % ticklabel style={font=\footnotesize},
    %
    % grid
    % none, major, minor, both
    grid=both, grid style={gray!20},
    % minor tick num=1, 
    % minor grid style={gray!20},
    % 
    % plot parameters
    %
    smooth, samples=100, no markers,
    ]
    % \usepackage{pgfplots}
    % \pgfplotsset{compat=newest, trig format=rad} 
    % \pgfplotsset{label style={font=\footnotesize}}

    \addplot[main, very thick, domain=-1:2] {(x+1)*(x-1)*(x-2) + 2};
    \node[main, above] at (axis cs:2,2) {\footnotesize \(f(x)\)};

  \end{axis}
  % \begin{scope}
  %   \fill[very thick, gray!5] plot[domain=-1:2.5, samples=100, smooth] (\x, {(\x+1)*(\x-1)*(\x-2) + 3}) -- (2.5,0) -- (-1,0);
  %   \draw[dotted] (-1,3) -- (-1,0);
  %   \draw[dotted] (2.5, 5.625) -- (2.5, 0);
  %   \draw[thick, teal] plot[domain=-1:2.5, samples=100, smooth] (\x, {(\x+1)*(\x-1)*(\x-2) + 3});
  %   \node[below] at (-1,0) {\footnotesize \(a\)};
  %   \node[below] at (2.5,0) {\footnotesize \(b\)};
  % \end{scope}
  %
  % \draw[very thin, ->] (-1.1,0) -- (2.6,0) node[right] {\footnotesize \(x\)};
  % \draw[very thin, ->] (0,0) -- (0,6) node[above] {\footnotesize \(y\)};
  %
  % \foreach \x in {-1,-0.5,...,2.5} {
  %   \draw (\x, 0.05) -- (\x, -0.05); % node[below] {\footnotesize \(\x\)};
  % }
  %
  % \foreach \y in {1,2,3,4,5} {
  %   \draw (0.05, \y) -- (-0.05, \y); % node[left] {\footnotesize \(\y\)};
  % }
\end{tikzpicture}
\end{document}
