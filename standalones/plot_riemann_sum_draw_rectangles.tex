\documentclass[tikz]{standalone}
\input{../tikz.tex.preamble}
\input{../typography.tex.preamble}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    axis lines = middle, % boxed, middle
    % axis on top,
    axis equal image,
    %
    % domain and range
    %
    % xmin={}, xmax={},
    ymin={0}, % ymax={},
    enlargelimits=true,
    %
    % axis labels
    %
    xlabel={\(x\)}, xlabel style={anchor=west},
    ylabel={\(y\)}, ylabel style={anchor=south},
    label style={at={(ticklabel* cs:1), font=\footnotesize}},
    %
    % ticks
    %
    xtick={-1, 0, 1, 2}, 
    ytick={0, 1, ..., 4}, yticklabels={0,1,,3,},
    % ticklabel style={font=\footnotesize},
    %
    % grid
    % none, major, minor, both
    grid=both, grid style={gray!20},
    % minor tick num=1, 
    % minor grid style={gray!20},
    % 
    % plot parameters
    %
    smooth, samples=100, no markers,
    ]
    % \usepackage{pgfplots}
    % \pgfplotsset{compat=newest, trig format=rad} 
    % \pgfplotsset{label style={font=\footnotesize}}

    \addplot[main, ultra thick, domain=-1:2] {(x+1)*(x-1)*(x-2) + 2};
    \node[main, above] at (axis cs:2,2) {\footnotesize \(f(x)\)};

  \end{axis}
\end{tikzpicture}
\end{document}
