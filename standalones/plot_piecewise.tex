%! TeX program = xelatex
\documentclass{standalone}
\usepackage{amsmath}
\usepackage{pgfplots}

\pgfplotsset{compat=1.18}

\author{Kelvin Chan}
% \date{\today}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    axis lines=center,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xlabel style={at={(ticklabel* cs:1)}, anchor=west}, % Place the xlabel at the end of the x-axis
    ylabel style={at={(ticklabel* cs:1)}, anchor=south}, % Place the ylabel at the end of the y-axis
    width=3.5in,
    height=2in,
    xmin=0,
    xmax=3.5,
    ymin=0,
    ymax=2, % Set ymax to 2
    enlargelimits=true,
    xtick={0, 1.57, 3.14},
    xticklabels={\(0\), \(\frac{\pi}{2}\), \(\pi\)},
    ytick={0, 1, 2},
    yticklabels={\(0\), \(1\), \(2\)},
    title={\footnotesize The graph of \(p(x)\) near \(\pi/2\)},
    % tick style={draw=none}, % Remove ticks
    % major tick length=2pt, % Hide tick lines
  ]
    \addplot[thick, domain=0:pi/2] {sin(deg(x))};
    \addplot[thick, domain=pi/2:pi] {1.5};

    % Add circle marker
    \draw[fill=white] (axis cs:pi/2,1) circle [radius=2pt];
  \end{axis}
\end{tikzpicture}
\end{document}
