\documentclass[tikz]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18} % Set the compatibility to version 1.18

\begin{document}

\begin{tikzpicture}
\begin{axis}[
    width=3in,
    height=5in,
    axis lines=center,
    xmin=-2.25, xmax = 3.25,
    ymin=-8.25, ymax = 12.25,
    xlabel={\(x\)},
    ylabel={\(y\)},
    % xtick={-2,-1,0,1,2,3},
    % ytick={-10,-5,0,5,10},
    grid=both,
    grid style={dotted}, % Set gray color for the grid lines
    % major grid style={line width=0.5pt,draw=gray!50},
    % minor tick num=1,
    % axis line style={shorten >=-10pt, shorten <=-10pt},
    ticklabel style={font=\footnotesize},
    xlabel style={at={(ticklabel* cs:1), font=\footnotesize}, anchor=west},
    ylabel style={at={(ticklabel* cs:1), font=\footnotesize}, anchor=south},
]
\addplot[color=black, domain=-2:3, samples=300, thick] {-(x+1)*(x-1)*(x-2)};
\end{axis}
\end{tikzpicture}

\end{document}
