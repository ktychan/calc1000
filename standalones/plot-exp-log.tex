\documentclass[tikz]{standalone}
\input{../tikz.tex.preamble}
\input{../typography.tex.preamble}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    xlabel={\(x\)},
    ylabel={\(y\)},
    xmin={-2}, xmax={2},
    ymin={-2}, ymax={2},
    enlargelimits,
    axis equal image,
    yticklabels={\empty},
    xticklabels={\empty},
    domain=-5:5,
    smooth, samples=100,
    grid=major,
    ]
  \addplot[dashed] coordinates { (-5,-5) (5,5) };
  \addplot[main, very thick]{2^x};
  \addplot[supp, very thick, domain=0.01:3]{ln(x)/ln(2)};
  \node[main, right] at (axis cs:1,2) {\(b^{x}\)};
  \node[supp, left] at (axis cs:2,1) {\(\log_{b}(x)\)};
  \end{axis}
\end{tikzpicture}

\begin{tikzpicture}
  \begin{axis}[
    xlabel={\(x\)},
    ylabel={\(y\)},
    xmin={-2}, xmax={2},
    ymin={-2}, ymax={2},
    enlargelimits,
    axis equal image,
    yticklabels={\empty},
    xticklabels={\empty},
    domain=-5:5,
    smooth, samples=100,
    grid=major,
    ]
  \addplot[dashed] coordinates { (-5,-5) (5,5) };
  \addplot[main, very thick]{exp(x)};
  \addplot[supp, very thick, domain=0.01:3]{ln(x)};
  \node[main] at (axis cs:1,2) {\(e^{x}\)};
  \node[supp] at (axis cs:2,1) {\(\ln(x)\)};
  \end{axis}
\end{tikzpicture}
\end{document}
