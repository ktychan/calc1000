%! TeX program = lualatex
\documentclass[../main.tex]{subfiles}
\begin{document} \section{Tangent lines and derivatives (at a fixed number)}
The derivative is a major topic in calculus and appears everywhere. \updatenote{Added the missing "The".}

\begin{minipage}{0.5\textwidth}
  \centering
  \includestandalone[page=1]{../standalones/derivative-intro}
  \url{https://www.desmos.com/calculator/la4liidanr}
\end{minipage}
\begin{minipage}{0.5\textwidth}
  \centering
  \includestandalone[page=2]{../standalones/derivative-intro}
  \url{https://www.desmos.com/calculator/xem9i18kfh}
\end{minipage}

The graphs of \(f(x)\) and \(g(x)\) are \emph{qualitatively} different at the marked point. Investigate by zooming in near the point in Desmos. 
\begin{itemize}
  \item The graph of \(f(x)\) near the marked point appears to be \underline{\hspace{1in}} if we zoom in enough.
  \item The graph of \(g(x)\) near the marked point does not appear to be \underline{\hspace{1in}} no matter how much we zoom in.
\end{itemize}

The derivative quantifies this qualitative difference.  

\begin{definition} \label{def:derivative-at-a-point}
  Let \(f(x)\) be a function defined on an \underline{\hspace{1in}} interval containing some constant \(a\). The \hlmain{derivative of \(f(x)\) at \(a\)}, denoted by \hlmain{\(f'(a)\)} is defined by
  \begin{align} 
    \label{eq:def-derivative-1}
    f'(a) &= \phantom{\lim_{h \to 0} \frac{f(a + h) - f(a)}{h}}\\
    \intertext{or equivalently}
    \label{eq:def-derivative-2}
    f'(a) &= \phantom{\lim_{x \to a} \frac{f(x) - f(a)}{x - a}}
  \end{align}
  provided that \underline{\hspace{2in}}.

  The \hlmain{tangent line for \(f(x)\) at \(x = a\)} is the line with slope \underline{\hspace{2cm}} and passes through the point \underline{\hspace{3cm}}. Its equation is
  \[
    y = \phantom{f'(a) (x - a) + f(a)} \hspace{1in}
  \]

  \textbook{page 217}
\end{definition}

\faStar{} Geometrically, \(f'(a)\) represents \underline{\hspace{4in}}.

Terminology: If \(f'(a)\) exists, then \(f\) is said to be \hlmain{differentiable at \(a\)}, and \(f(x)\) has a tangent line at \(x = a\). If \(f'(a)\) does not exist, then \(f\) is said to be \hlmain{not differentiable at \(a\)}, and \(f(x)\) does not have a tangent line at \(x = a\). \updatenote{"Terminologies" corrected to "Terminology".}
\clearpage

The limit definition of derivatives, e.g. Equation~\eqref{eq:def-derivative-1} or \eqref{eq:def-derivative-2}, is fundamentally important and \hlmain{useful for unfamiliar problems}. \updatenote{Added the missing comma closing the "e.g...." clause.} If you know differentiation rules, the exercises in this section may seem overly complicated. However, it is the purpose of this section to help you understand that, fundamentally, the derivative is a limit. Everything you know about limits can be used to solve differentiation problems, especially the unfamiliar ones.  \label{comment:derivative-definition}

\faComment{} (Reflection) Every single exercise in this section starts with exactly the same step. What is it?

\begin{example} 
  Find the derivative of \(x^{2}\) at \(3\) using Equation~\eqref{eq:def-derivative-1} or \eqref{eq:def-derivative-2}. Write down the equation of the tangent line at \(x = 3\).

  \blanklines{20}
\end{example}

\begin{example}
  Show that \(y = |x|\) is not differentiable at \(0\).

  \blanklines{20}
\end{example}

\clearpage
\begin{exercise}[The slingshot] \label{ex:the-slingshot}
  Consider \(f(x) = \begin{cases} x^{2} &\text{if } x \le 2\\ 4x - 4& \text{if } x > 2 \end{cases}\). Does \(f'(2)\) exist?

  \blanklines{25}
\end{exercise}

\begin{exercise}
  Does \(f(x) = x^{1/3}\) have a derivative at \(0\)?  Justify your answer by calculating \(f'(x)\) using the limit definition, i.e., Equation~\eqref{eq:def-derivative-1} or \eqref{eq:def-derivative-2}.
  Hint: Remember Exercise~\ref{ex:limit-cube-root}?

  \blanklines{20}
\end{exercise}
\clearpage

Want to see how fundamentally important the limit definition is? 
\begin{exercise}[Midterm Fall 2024] \label{ex:midterm-f2024-squeezed-derivative}
  Suppose we have a function \(f(x)\) defined for all real numbers, and suppose that we know
  \[
    \frac{x^{6}}{36} - \frac{x^{4}}{3} + x^{2} \le f(x) \le \sin^{2}(x) \quad\text{ for all } x \text{ in } (-1, 1).
  \]

  Find the derivative of \(f(x)\) at \(x = 0\).  \quad Hint: The top of page \pageref{comment:derivative-definition}.

  {\footnotesize This is a hard short-answer problem. Two hints were provided in the midterm for this question so that the analysis and the calculation could be completed within a reasonable amount of time. One of the hints is that \(f(0) = 0\). The solution that gets the full credit is not short. The solution where one simply differentiates \(x^{6}/36 - x^{4}/3 + x^{2}\) and \(\sin^{2}(x)\) at \(0\) is invalid and receives \(0\) mark.}

  

  \blanklines{40}
\end{exercise}
\clearpage

\end{document}

