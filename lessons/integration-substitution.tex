%! TeX program = lualatex
\documentclass[../main.tex]{subfiles}
\begin{document} \section{The substitution rule}

We have seen throughout the course that \hlmain{a change of variable} can \emph{transform} a complex problem into a simpler one. The substitution rule performs such a change of variable in integration.

\begin{thm}
  {If {\color{main} \(u = g(x)\)} is differentiable and \(f\) is continuous on the range of \(g\), then}
  \begin{equation} \label{eq:sub-definite}
    \int_{a}^{b} f( {\color{main} g(x)} ) \, {\color{supp} g'(x) \;dx} = \int_{u(a)}^{u(b)} f({\color{main} u}) \;{\color{supp}du}.
  \end{equation}

  For indefinite integrals, simply do nothing for the bounds: 
  \begin{equation} \label{eq:sub-indefinite}
    \int f( {\color{main} g(x)} ) \, {\color{supp} g'(x) \;dx} = \int f(u) \;{\color{supp}du}.
  \end{equation}

  \textbook{Theorem 5.7 on page 584 and Theorem 5.8 on page 589}
\end{thm}

The substitution rule works by undoing\footnote{See pages 584 and 589 of the textbook for a short theoretical explanation if you are interested.} the chain rule. 
Once a substitution \(u\) is chosen, substitute \(dx = \frac{du}{u'}\), simplify and change the leftover into a function of \(u\). Update the bounds for definite integrals. 

\medskip
\begin{example} 
  Evaluate \(\int_{1}^{2} x e^{x^{2}} \;dx\).

  \blanklines{25}
\end{example}
\clearpage

\textbf{Strategy} for choosing \(u\). \hlattn{Recognize} a \hlmain{pair} of functions (almost) related by differentiation and choose \(u\) to be the antiderivative.  An extra clue is that \(u\) could be an inside function of a composite function. Sometimes, there is more than one choice for \(u\) that works. \updatenote{Subject-verb agreement: "there are ... that work" corrected to "there is ... that works" ("more than one" takes a singular verb).}  Be brave and go with some trial and error to develop some intuition!

\faExclamationTriangle{} The integration table on page~\pageref{sec:integration-formulas} \hlmain{guarantees correctness}. \updatenote{The page reference pointed to the wrong label (one that was actually attached to the "Differentiation formulas" lesson); corrected to the label on the "Integration formulas" lesson.} If something is not in the integration table, then you should manipulate it using properties of integrals and/or substitution to one or more known integrals.

\begin{example} \label{ex:u-sub-1}
  Evaluate \(\int x^{2} \sqrt{1 + x^{3}} \;dx\).

  \blanklines{45}
\end{example}
\clearpage

Notice how the strategy on page~\pageref{ex:u-sub-1} is used here.
\begin{exercise}
  Evaluate \(\int \sin(x) e^{\cos(x)} \;dx\).  

  \blanklines{20}
\end{exercise}

\begin{exercise}[Final exam, Fall 2024]
  Let \(D = \int_{0}^{\pi/4} \tan(x) \sec^{2}(x) \;dx\). In the box below, write down an expression for \(D\). Perform all necessary differentiation and integration.

  \begin{tikzpicture}
    \draw (0,0) rectangle (5in,2); 
    \node[right] at (0.5, 0.5) {\(D = \) \underline{\hspace{4in}}};
  \end{tikzpicture}

  (Here is some space for rough work. Please note that the answer outside of the box \emph{will not be graded}. \updatenote{Added the missing article "the" before "answer".})

  \blanklines{20}
\end{exercise}

% When there is no obvious choice for a substitution, try reorganizing the integrand first.
% \begin{exercise}
%   Evaluate \(\int_{0}^{\pi/4} \tan(\theta) \;d\theta\). \hfill{}{The final answer is \(\ln(2)/2\).}
%
%   Hint: Rewrite \(\tan(\theta) = \frac{\sin(\theta)}{\cos(\theta)}\) and find a substitution \(u\). 
%
%   \blanklines{30}
% \end{exercise}

% In Example~\ref{ex:u-sub-1}, one of the many choices for \(u\) is guided by the composite function \(\sqrt{1 + x^{3}}\). Let's look at a variation where the previous strategy does not work. At a glance, Examples~\ref{ex:u-sub-1}~and~\ref{ex:u-sub-2} look very similar. However, they require different problem-solving ideas.
%
% \textbf{Strategy \(2\)} for choosing \(u\): Linear functions are special. In such situations, choose \(u\) to be the linear function and rewrite \(x\) in terms of \(u\) to perform the substitution.
% \begin{exercise} \label{ex:u-sub-2}
%   Evaluate \(\int \frac{4x^{2} + 1}{2x - 1} \;dx\).
%
%   \blanklines{25}
% \end{exercise}
%
% \begin{exercise}
%   Evaluate \(\int_{1}^{0} \frac{2x}{1 + x^{2}} \;dx\). \hfill{}{(The answer is \(-\ln(2)\))}
%
%   \blanklines{15}
% \end{exercise}
%
% \clearpage
% When there is no obvious choice for a substitution, try reorganizing the integrand first.
% \begin{example}
%   Evaluate \(\int_{0}^{\pi/4} \tan(\theta) \;d\theta\). \hfill{}{(The answer is \(\ln(2)/2\))}
%
%   Hint: Rewrite \(\tan(\theta) = \frac{\sin(\theta)}{\cos(\theta)}\) and find a substitution \(u\). See below if you get \(-\ln(\pi/4) + \ln(0)\).
%
%   \blanklines{20}
%   Common misadventure: If you get \(-\ln(\pi/4) + \ln(0)\) (our number sense should tell us this is obviously wrong), then you might have forgotten to apply \(u\) to the upper and lower bounds of the original integral.
% \end{example}
%
% \medskip
% The substitution rule works in more abstract context.
% \begin{example}
%   Suppose \(\int_{0}^{1} f(x) \;dx = -5\). Evaluate \(\int_{0}^{\pi/4} f(\tan(x)) \sec^{2}(x) \;dx\).
%
%   \blanklines{15}
% \end{example}
\end{document}
