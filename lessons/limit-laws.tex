%! TeX program = lualatex
\documentclass[../main.tex]{subfiles}
\begin{document} \section{Algebraic techniques for evaluating limits}
Direct substitution and limit laws are the main algebraic tools for evaluating limits. 

\faStar{} Basic functions --- polynomials, rational functions, trigs, \(e^{x}\), \(\ln(x)\) --- share a common feature on their graphs. If a constant \(a\) is in the domain of these functions, then the graph of \(f(x)\) will always \enquote{go through} the point \((a,f(a))\). In other words, as long as \(f(x)\) is one of the basic functions and \hlsupp{\(a\) is in its domain}, then \(\lim_{x \to a} f(x) = f(a)\). This is called \hlmain{direct substitution}.

However, if \(f(x)\) is built from basic functions using algebraic operations, we need the limit laws below.

\begin{thm}[limit laws] \label{thm:limit-laws}
  Suppose \(\lim_{x \to a} f(x)\) and \(\lim_{x \to a} g(x)\) \hlwarn{are finite numbers}. Then
  \begin{enumerate}[label=(\arabic*)]
    \item \(\lim_{x \to a} [f(x) + g(x)] = \lim_{x \to a} f(x) + \lim_{x \to a} g(x)\) \hfill (sum)
    \item \(\lim_{x \to a} [f(x) - g(x)] = \lim_{x \to a} f(x) - \lim_{x \to a} g(x)\) \hfill (difference)
    \item \(\lim_{x \to a} [c \; f(x)] = c \lim_{x \to a} f(x)\), where \(c\) is a constant \hfill (constant multiple)
    \item \(\lim_{x \to a} [f(x) \; g(x)] = \lim_{x \to a} f(x) \cdot \lim_{x \to a} g(x)\) \hfill (product)
    \item \(\lim_{x \to a} \frac{f(x)}{{\color{warn} g(x)}} = \frac{\lim\limits_{x \to a} f(x)}{{\color{warn} \lim\limits_{x \to a} g(x)}}\), \quad \hlwarn{if {\(\lim_{x \to a}g(x) \ne 0\)}} \hfill (quotient)
    \item \(\lim_{x \to a} \bigg[f(x)\bigg]^{n} = \bigg[ \lim_{x \to a} f(x) \bigg]^{n}\) \hfill (power)
    \item \(\lim_{x \to a} \sqrt[n]{f(x)} = \sqrt[n]{\lim_{x \to a} f(x)}\), \quad if \(n\) is even, we assume {\(\lim_{x \to a} f(x) \ge 0\)} \hfill (root)
  \end{enumerate}

  \textbook{Theorem 2.5 on page 161}
\end{thm}

\faStar{} All limit laws apply to \hlmain{two-sided} limits and to \hlmain{one-sided} limits of the same type.

\begin{example}
  Evaluate \(\lim_{x \to 1} \sqrt[3]{\frac{x^{2} + 1}{x+1}} - 2 (4x - 3)^{100}\).

  \blanklines{15}
\end{example}
\clearpage

\begin{sidefigure}[0.40]{%
  \includestandalone[page=11, width=\sidefigwidth]{../standalones/mindmap-limits}}
  The figure on the right shows \emph{one way} to model the decision-making process for evaluating limits. We start with when to try direct substitution and when to return to the basic concepts from the last lesson.
\end{sidefigure}

\begin{example} \label{eg:limit-law-abstract}
  Suppose \(\lim_{x \to -3} f(x) = 4\). 

  Evaluate \(\lim_{x \to -3} \left( f(x) \right)^{3/2}\) if possible.

  \blanks{15}
\end{example}

Sometimes we are given a combination of graphical and algebraic information.

\begin{exercise}[Midterm, Fall 2023]
  The graph of a function \(f(x)\) is given on the right. 

  \begin{sidefigure}{%
    \includestandalone[page=4]{../standalones/plot-limits-complex}}
    Find \(\lim_{x \to 0} \frac{x^{2} + f(x) + 3}{\sqrt[3]{3 f(x) + 5}}\).
    \begin{enumerate}[label=\Alph*.]
      \item \(0\)
      \item \(\sqrt[3]{4}\)
      \item \(2\)
      \item \(\sqrt[3]{3}\)
      \item \(\sqrt[3]{2}\)
    \end{enumerate}
  \end{sidefigure}

  \blanks*
\end{exercise}
\clearpage

\begin{example}
  The graphs of \(f(x)\) and \(g(x)\) are given below. 

  \begin{center}
    \includestandalone[page=1]{../standalones/plot-limits-complex}
  \end{center}

  Evaluate these limits.
  \[
    \lim_{x \to 2} \Big( f(x) + g(x) \Big) = \underline{\hspace{1in}},
    \qquad
    \lim_{x \to 2} \Big( g(x) - f(x) \Big) = \underline{\hspace{1in}},
  \]
  % Answer: the first one does not exist and the second one is 3.

  \blanks*
\end{example}

\begin{example} \label{eg:limit-composition-abstract}
  The graphs of \(f(x)\) and \(g(x)\) are given below. 

  \begin{center}
    \includestandalone[page=2]{../standalones/plot-limits-complex}
  \end{center}

  Evaluate these limits.
  \[
    \lim_{x \to 2} f(g(x)) = \underline{\hspace{1in}},
    \qquad
    \lim_{x \to 2} g(f(x)) = \underline{\hspace{1in}},
  \]
  % Answer: 

  \blanks*
\end{example}
\clearpage

Here are more variations of the same idea for practice. 

\begin{exercise}[Midterm, Fall 2023]
  The graph of a function \(f(x)\) is given on the right. 
  
  Find \(\lim_{x \to 2} \Big(f(x) + 2 f(-x)\Big)\).

  \begin{sidefigure}{%
    \includestandalone[page=4]{../standalones/plot-limits-complex}}
    \begin{enumerate}[label=\Alph*., itemsep={2ex}]
      \item \(6\)
      \item \(0\)
      \item \(-6\)
      \item \(2\)
      \item Does not exist.
    \end{enumerate}
  \end{sidefigure}

  \blanks*
\end{exercise}


\begin{exercise} \label{ex:limit-composition-shifted}
  The graphs of \(f(x)\) and \(g(x)\) are given below. 

  \begin{center}
    \includestandalone[page=3]{../standalones/plot-limits-complex}
  \end{center}

  Evaluate these limits.
  \[
    \lim_{x \to -1} f(g(x) + 1) = \underline{\hspace{1in}},
    \qquad
    \lim_{x \to -1} g(f(x + 1)) = \underline{\hspace{1in}},
  \]
  % Answer: 

  \blanks*
\end{exercise}
\clearpage

\begin{sidefigure}{
\includestandalone[page=5, width=\sidefigwidth]{../standalones/mindmap-limits}
}
  Now we evaluate limits for which the full algebraic information of the function is given. The two \(\frac{\cdots}{0}\) decision-making branches follow directly from the quotient law in Theorem~\ref{thm:limit-laws}. When the limit of the denominator is \(0\), the quotient law does not apply and we need to \emph{try} other algebraic techniques.
\end{sidefigure}

\begin{example}
  Evaluate \(\lim_{x \to 2} f(x)\) and \(\lim_{x \to 1} f(x)\) given
  \[
    f(x) = 
    \begin{cases}
      \sqrt{x - 1} + 1, &x > 1\\[1ex]
      \frac{2x}{x + 1}, &x < 1
    \end{cases}.
  \]

  \blanks{15}
\end{example}

\begin{example} \label{eg:limit-constant-over-0}
  Evaluate \(\lim_{x \to 1} \frac{x}{x - 1}\).

  \blanklines{10}
\end{example}

\clearpage
\begin{sidefigure}{
\includestandalone[page=6, width=\sidefigwidth]{../standalones/mindmap-limits}
}
If the limits of the numerator and the denominator are both \(0\), we need to try a few algebra techniques to turn it into another form.
\end{sidefigure}

\begin{example} \label{eg:limit-cancellation}
  Evaluate \(\lim_{t \to 2} \frac{t^{2} - 3t + 2}{t - 2}\).

  \blanks{10}
\end{example}

\begin{example} \label{eg:limit-rationalization}
  Evaluate \(\lim_{x \to 0} \frac{\sqrt{x^{2}+1}-1}{x^{2}}\).

  \blanks{13}
\end{example}

\begin{exercise} \label{ex:limit-rationalization-2}
  Evaluate \(\lim_{x \to 1} \frac{\sqrt{x + 3}-2}{\sqrt{x + 8} - 3}\).

  \blanks*
\end{exercise}
\clearpage

\begin{sidefigure}{
\includestandalone[page=4, width=\sidefigwidth]{../standalones/mindmap-limits}
}
  We now discuss change of variable in the context of limits. Example~\ref{eg:limit-composition-abstract} showed a change of variable in pictures; here we apply the same idea to concrete functions.

  \faExclamationTriangle{} When performing a change of variable, remember to update the limit of the variable.
\end{sidefigure}

\begin{example} \label{eg:limit-change-of-variable}
  Evaluate \(\lim_{x \to 1} \frac{(\sin(3x - 3))^2}{x - 1} \).

  \blanks*
\end{example}

\faStar{} You may use take these three limits for granted, i.e., use them without proof in quizzes, assignments and exams.
\begin{center}
  \includegraphics[page=1]{../standalones/plots-limits-well-known}
  \hfill{}
  \includegraphics[page=2]{../standalones/plots-limits-well-known}
  \hfill{}
  \includegraphics[page=3]{../standalones/plots-limits-well-known}
\end{center}
\clearpage

Here are more change-of-variable exercises that do not appear in our textbook.

\begin{exercise}
  Evaluate \(\lim_{x \to 0} \frac{\sqrt{x^{2} + 1} - 1}{x^{2}}\) using a change of variable \(u = \sqrt{x^{2} + 1}\).

  \blanks*
\end{exercise}

\begin{exercise} \label{ex:limit-cube-root}
  Evaluate \(\lim_{x \to 1} \frac{\sqrt[3]{x} - 1}{\sqrt{x} - 1}\) using a change of variable \(u = \sqrt[6]{x}\).

  {\footnotesize Factorization hint: \(a^{3} - b^{3} = (a-b)(a^{2} + ab + b^{2})\). Rationalization also works, but the solution is slightly longer.}

  \blanks*
\end{exercise}
\clearpage

Exercise~\ref{ex:limit-textbook-2.28} is a subtle variation on the theme of Examples~\ref{eg:limit-cancellation} and \ref{eg:limit-rationalization}, but it challenges our algebra skills. It is good practice once you are comfortable with the other skills. The hint at the bottom of the page can get you started.
\begin{exercise}[Textbook Example~2.20] \label{ex:limit-textbook-2.28}
  Evaluate \(\lim_{x \to 0} \left( \frac{1}{x} + \frac{5}{x(x-5)}\right)\) or explain why it does not exist.

  \blanklines{20}
\end{exercise}

\vfill{}
{\scriptsize Hint to Exercise~\ref{ex:limit-textbook-2.28}}. \newline
\begin{tikzpicture}[scale=1]
  \node[rotate={pi}, inner sep=0pt] {\parbox{\linewidth}{\scriptsize Notice the sum law cannot be used because \(\lim_{x \to 0} 1/x\) does not exist. Remember that the limit law can only be used to break up a larger limit into two smaller limits when both smaller limits exist. The trick is to rewriting the function in Exercise~\ref{ex:limit-textbook-2.28} as a single quotient. Can we be sure this works? We can't! It takes a bit of trial and error.}};
\end{tikzpicture}
\clearpage

\end{document}
