%! TeX program = lualatex
\documentclass[../main.tex]{subfiles}
\begin{document} \section{Review of exponential functions and logarithms}
A function \(b^{x}\), where \(b\) is a constant so that \(b > 0\) and \(b \ne 1\), is called an \hlmain{exponential function with base \(b\)} and exponent \(x\). Within the context of our textbook, every exponential function \(b^{x}\) has an \hlsupp{inverse} called the \hlmain{logarithmic function with base \(b\)}, denoted by \(\log_{b}(x)\). \newline
{\scriptsize Some authors allow \(1^{x}\) to be an exponential function. However, \(1^{x}\) is a constant function and does not have an inverse.}

\begin{minipage}{0.6\textwidth}
  \centering
  \begin{tabular}{l|p{1in}|p{1in}}
    & Domain & Range \\\midrule
    \(b^{x}\) & & \\[2ex]\midrule
    \(\log_{b}(x)\) & & \\[2ex]
  \end{tabular}
\end{minipage}
\begin{minipage}{0.4\textwidth}
  \centering
  \includestandalone[page=1]{../standalones/plot-exp-log}
\end{minipage}

Let's quickly recall exponents rules. \(e^{a}e^{b} = \underline{\hspace{1in}}\) and \((e^{a})^{b} = \underline{\hspace{1in}}\) for any \(a,b\).
\blanklines{5}

The number \(e\) is an important constant defined by \(e = \lim_{x \to \infty} \left( 1 + \frac{1}{x} \right)^{x}\). Whenever you see the limit on the right-hand side, you can write down \(e\) as the answer with justification ``by definition.'' 
\bigskip

\begin{definition} \label{def:exp-ln}
  The \emph{natural} exponential function is \(e^{x}\) and the \emph{natural} logarithm is \(\ln(x) = \log_{e}(x)\).  They are inverses of each other:
  \begin{equation} \label{eq:exp-log}
    e^{\ln(x)} = x \text{ on } \underline{\hspace{1in}} \quad\text{and}\quad \ln(e^{x}) = x \text{ on } \underline{\hspace{1in}}.
  \end{equation}

  The goal of this lesson is to consolidate all of our knowledge of exponential and exponential-like functions to Equation~\eqref{eq:exp-log} and the ``\hlmain{change base to \(e\)}'' trick
  \begin{equation} \label{eq:exp-change-of-base}
    b = \underline{\hspace{1in}} \quad\text{and}\quad b^{x} = \underline{\hspace{1in}}, \quad\text{ but need } b > 0.
  \end{equation}
\end{definition}
\faStar{} Equation~\eqref{eq:exp-change-of-base} offers a \hlattn{universal method}, called \hlmain{change base to \(e\)}, to deal with all exponential or exponential-like functions. The typical example is \(x^{x}\).

\blanklines{5}

\clearpage

% \begin{example} \label{ex:change-base-to-e}
%   Let \(b\) be a constant so that \(b > 0\) and \(b \ne 1\).  Change \(b^{x}\) and \(\log_{b}(x)\) to base \(e\). 
%
%   \blanklines{15}
% \end{example}

\begin{example}
  Solve the equation \(\frac{e^{2x}}{2^{x-1}} = 3\). 

  \blanklines{25}
\end{example}

\begin{exercise}
  Solve the equation \(5^{x/3 - 1} = e\). 

  \blanklines{20}
\end{exercise}
\clearpage

Formulas in Equation~\eqref{eq:log-formulas} can be deduced from Equation~\eqref{eq:exp-change-of-base} and can help solve problems \emph{quickly}. 
\begin{equation} \label{eq:log-formulas}
  \log_{b}(a) = \frac{\ln(a)}{\ln(b)},
  \quad
  \ln(a^{x}) = x \ln(a),
  \quad
  \ln(ab) = \ln(a) + \ln(b),
  \quad
  \ln(a/b) = \ln(a) - \ln(b).
\end{equation}

We demonstrate \hlmain{strategic thinking} with and without the formulas in \eqref{eq:log-formulas}.  
\begin{example}
  Solve for \(y\) given that \(\log_{2}\left(\frac{y}{\sqrt{x}}\right) = 3\).

  \blanklines{30}
\end{example}

\begin{exercise}
  Solve the equation \(7^{x - 1} = \log_{2}(8)\).

  \blanklines{10}
\end{exercise}
\clearpage

% These exercises help you get a feel for the usefulness of change-base-to-\(e\), i.e., Equation~\eqref{eq:exp-change-of-base}. 
%
% \begin{exercise} 
%   Rewrite the function \(\left(1 + \frac{1}{2x}\right)^{3x}\) so that it is in base \(e\), i.e., \hlmain{change base to \(e\)}. 
%
%   \blanklines{5}
%
%   (Preview). Evaluate \(\lim_{x \to \infty} \left(1 + \frac{1}{2x}\right)^{3x}\). There are (at least) two methods to solve this problem. 
%   \begin{enumerate}[wide, label={Method (\arabic*).}]
%     \item Combine the above change-base-to-\(e\) and l'H\^opital's rule.  This method is widely applicable but requires some algebra steps. \updatenote{"methods" corrected to "method".}
%
%       \blanklines{10}
%
%     \item Perform a change of variable \(u = 2x\) and use the property of continuous functions.  This method is fast but requires us to recognize \(e\) from the limit stated on page~\pageref{def:exp-ln}. \updatenote{"methods" corrected to "method"; changed \texttt{\textbackslash ref} to \texttt{\textbackslash pageref} so this points to the actual page number, matching how the same label is used in lHopitals-rule.tex.}
%
%       \blanklines{5}
%   \end{enumerate}
% \end{exercise}
%
% \begin{exercise} 
%   Rewrite the function \(\left(\frac{1}{x + 2}\right)^{\sin(x)}\) so that it is in base \(e\), i.e., \hlmain{change base to \(e\)}.  
%
%   \blanklines{5}
%
%   (Preview) Find the derivative of \(\left(\frac{1}{x+2}\right)^{\sin(x)}\). Assume \(x > 0\) so that the function is well-defined.
%
%   \blanklines{10}
% \end{exercise}
% \clearpage

\begin{exercise}[Summary of exponential and logarithms rules]
  First, remember the universal method, \hlmain{change base to \(e\)}, for dealing with exponential or exponential-like functions.
  \[
    \begin{array}{rclcrcl}
      b^{x} &=& \underline{\hspace{1in}} 
            &\hspace{1em} & 
      \log_{b}(x) &=& \underline{\hspace{1in}} \\[2ex]
    \end{array}
  \]

  Once everything is in base \(e\), then use the following rules.
  \[
    \begin{array}{rclcrclcrcl}
       && & & 
      \ln(e) &=& \underline{\hspace{1in}} &\hspace{1em} & \\[2ex]
      e^{\ln(x)} &=& \underline{\hspace{1in}} 
                 &\hspace{1em} & 
      \ln(e^{x}) &=& \underline{\hspace{1in}} 
                 &\hspace{1em} & 
      \ln(b^{x}) &=& \underline{\hspace{1in}} 
      \\[2ex]
      e^{a} e^{b} &=& \underline{\hspace{1in}}
                  &\hspace{1em} & 
      \ln(ab) &=& \underline{\hspace{1in}} \\[2ex]
      \frac{e^{a}}{e^{b}} &=& \underline{\hspace{1in}}
                          &\hspace{1em} & 
      \ln(a/b) &=& \underline{\hspace{1in}} \\[2ex]
    \end{array}
  \]
\end{exercise}

Here are some exercises targeting the use of identities for exponential and logarithmic functions. 
\begin{exercise}
  Solve the equation \(\ln \sqrt{x - 1} = 0\). 

  \blanklines{10}
\end{exercise}

\begin{exercise}
  Simplify \(\frac{\log_{6}(3)}{\log_{7}(49)}\).

  \blanklines{10}
\end{exercise}
\end{document}

