%! TeX program = lualatex
\documentclass[../main.tex]{subfiles}
\begin{document} \section{Area between curves}
To calculate the area between two curves, \hlmain{use definite integrals as a language to describe the desired area and evaluate the resulting definite integral.} In such problems, \hlsupp{area means total area.}
\begin{figure}[H]
  \includestandalone[page=1]{../standalones/plot-area-between-curves}
  % \includestandalone[page=2]{../standalones/plot-area-between-curves}
  % \includestandalone[page=3]{../standalones/plot-area-between-curves}
\end{figure}

% \begin{example}
%   Set up a definite integral representing the area of the region between two curves \hlmain{\(y = f(x)\)} and \hlsupp{\(y = g(x)\)} and between lines \(x = -2\) and \(x = 1\).
%
%   \blanklines{35}
% \end{example}

% \begin{example}
%   Find the area bounded between \(\sin(x)\) and \(\cos(x)\) over the interval \([0, \pi]\).
%
%   \includestandalone[page=4]{../standalones/plot-area-between-curves}
%
%   \blanklines{25}
% \end{example}
% \clearpage

% What if there is no sketch?
\begin{example}
  Find the area of the region enclosed by the curves \(y = x^{2}\) and \(y = x^{3} - 2x\).

  \blanklines{30}
\end{example}
\clearpage

\begin{exercise}
  Find the area of the region bounded by the graphs of \(-2x + 1\) and \(x^{2}\). \updatenote{"the area of bounded by" corrected to "the area of the region bounded by".}

  \blanklines{45}
\end{exercise}
\end{document}
