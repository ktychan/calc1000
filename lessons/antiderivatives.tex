%! TeX program = lualatex
\documentclass[../main.tex]{subfiles}
\begin{document} \section{Antiderivatives}
We set up terminologies and notations for integration.

\begin{definition}
  If \(f(x)\) and \(F(x)\) are related by
  \[
    f(x) = F'(x) \text{ for every } x \text{ in the domain of } f(x),
  \]
  then we \emph{say} \(F(x)\) is \emph{an} antiderivative of \(f(x)\).
\end{definition}

Let's make sense of the above definition.
\begin{example}
  Consider \(f(x) = 2xe^{x^{2}}\) and \(g(x) = e^{x^{2}}\). Is \(f\) an antiderivative of \(g\)? Is \(g\) an antiderivative of \(f\)?

  \blanklines{10}
\end{example}

\begin{exercise}
  Is \(\ln(x)\) an antiderivative of \(1/x\)? Justify your answer using the definition above.

  \blanklines{10}
\end{exercise}

\begin{exercise}
  Consider \(f(x) = 3x^{2}\).

  \begin{enumerate}
    \item Find an antiderivative of \(3x^{2}\) using the definition.
    \item List infinitely many antiderivatives of \(3x^{2}\).
  \end{enumerate}
  \blanklines{10}
\end{exercise}
\clearpage

\begin{definition}
  The \hlmain{indefinite integral} of \(f\) is the most general antiderivative of \(f(x)\), denoted by 
  \[
    \int f(x) \;dx.
  \]
  If \(F\) is \emph{an} antiderivative of \(f\), then we get an explicit expression \(\int f(x) \;dx = F(x) + C\) where \(C\) represents a general but unknown constant.

  \textbook{Definition, page 487}
\end{definition}

\begin{example}
  The definition allows us to evaluate simple integrals.

  \begin{multicols}{2}
    \begin{enumerate}[itemsep={2cm}]
      \item \(\int 0 \;dx = \phantom{\text{(any constant)} + C}  \)
      \item \(\int e^{x}\;dx = \phantom{e^{x} + C}\)
      \item \(\int 10 \;dx = \phantom{10x + C}\)
      \item \(\int x^{5} \;dx = \)
      \item \(\int x^{3} \;dx = \)
      \item \(\int \frac{1}{\sqrt{x}} \;dx = \)
      \item \(\int \sqrt{x} \;dx = \)
      \item \(\int \sin(x) \;dx = \)
      \item \(\int \cos(x) \;dx = \)
      \item \(\int \frac{1}{1 + x^{2}} \;dx = \)
      \item \(\int \sec^{2}(x) \;dx = \)
      \item \(\int \tan(x)\sec(x) \;dx = \)
    \end{enumerate}
  \end{multicols}
  
\end{example}
\clearpage

Algebraic properties of integrals break up complex integrals into simple ones.

\begin{thm}
  \begin{align*}
    \int c f(x) \;dx 
    &= c \int f(x) \;dx && \text{(constant multiple)}\\
    \int f(x) + g(x) \;dx 
    &= \int f(x) \;dx + \int g(x) \;dx && \text{(sum)}\\
    \int f(x) - g(x) \;dx 
    &= \int f(x) \;dx - \int g(x) \;dx && \text{(difference)}
  \end{align*}

  \textbook{Theorem~4.16, page 491}
\end{thm}

\begin{example}
  Evaluate \(\int \sin(x) - \sec^{2}(\pi/4) \;dx\). 

  \blanklines{5}
\end{example}

\begin{exercise}
  Evaluate \(\int (x-1)(x+2) \;dx\).

  \blanklines{15}
\end{exercise}

\begin{exercise}
  Suppose \(F'(x) = (x-1)(x+2)\) and \(F(-1) = 0\).  Find \(F(x)\).

  \blanklines{10}
\end{exercise}



\clearpage
\begin{exercise}
  Find the function \(F(x)\) satisfying \(F'(x) = \frac{2}{\sqrt{1 - x^{2}}}\) and \(F(0) = 2\).

  \blanklines{15}
\end{exercise}

\begin{exercise}
  Find the function \(F(x)\) satisfying \(F'(x) = \sin(x) + \frac{x^{2}}{3}\) and \(F(0) = 0\).

  \blanklines{15}
\end{exercise}
\end{document}
